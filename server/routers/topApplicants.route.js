const express = require('express');
const Applicant = require('../models/Applicant'); // Adjust path as needed

const router = express.Router();

// GET /top-applicants/:jobId?min=20&max=50
router.get('/top-applicants/:jobId',);

module.exports = router;